name = "Puneet" # it is the string
# Strings are immutable 
nameshort = len(name)
print(nameshort)

namesh = name[0:3] # Start from index 0 all the way till 3 (excluding 3)
print(namesh)

character1 = name[-5]
print(character1)